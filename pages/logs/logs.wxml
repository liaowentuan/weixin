<!--logs.wxml-->
<view class="container log-list">
  <block wx:for="{{logs}}" wx:for-item="log" wx:key="{{$index}}">
    <!-- for循环需要增加wx:key='{{$index}}' 提升效率 -->
    <text class="log-item">{{index + 1}}. {{log}}</text>
  </block>
</view>
